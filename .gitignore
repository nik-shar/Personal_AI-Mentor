# Environment variables & secrets
.env
.env.*
!.env.example

# Local PostgreSQL Data Directory
.pgdata/

# Local Vector Store Data
chroma_store/

# Local Databases & Checkpoints
*.db
*.sqlite
*.sqlite3
data/checkpoints.sqlite*

# Generated memory artifacts (regenerate anytime)
data/memory.md
data/memory_backups/
memory.json

# Roadmap store: recoverable deletions land here (`delete_roadmap` moves a folder
# in rather than unlinking it). Local-only — never committed.
learning/topics/.trash/

# Python-generated & build files
__pycache__/
*.py[oc]
*.pyc
*.pyd
build/
dist/
wheels/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
ENV/
env/

# IDE & Editor files
.vscode/
.idea/
.zed/
*.swp
*.swo
.DS_Store
Thumbs.db

# Personal files & artifacts
*.pdf
about_me.md
challenges.md
profile.yaml

# Obsidian local settings (if applicable)
.obsidian/

# Log files
*.log

# Node / Next.js
node_modules/

# Vendored Pi agent harness — a separate upstream git repo
# (github.com/earendil-works/pi). Never commit into this repo: it would
# create an embedded repo / huge gitlink. Run it from ./pi via ./pi-test.sh.
pi/
